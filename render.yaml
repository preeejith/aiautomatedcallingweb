services:
  - type: web
    name: elevenlabs-outbound-calling-backend
    env: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 18.x
